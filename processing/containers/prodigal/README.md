# Prodigal Container

Build:

```bash
./processing/containers/prodigal/build_container.sh
```

Test:

```bash
docker run --rm prodigal-annotation:1.0 prodigal -v
```

This image is intended as the Apptainer source for `prodigal-annotation:1.0`.
